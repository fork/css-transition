{
  "name": "css-transition",
  "version": "0.1.0",
  "description": "Apply a CSS class for each stage of a CSS transition",
  "main": "dist/index.js",
  "author": "Fork Unstable Media GmbH <obj@fork.de>",
  "license": "MIT",
  "scripts": {
    "start": "parcel demo/index.html --open",
    "build": "parcel build --no-minify --global CSSTransition src/index.js"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged; npm run build; git add dist/"
    }
  },
  "lint-staged": {
    "src/*.js": ["eslint --quiet --fix", "git add"]
  },
  "devDependencies": {
    "eslint": "^5.8.0",
    "eslint-config-airbnb-base": "^13.1.0",
    "eslint-config-prettier": "^3.1.0",
    "eslint-plugin-import": "^2.14.0",
    "eslint-plugin-prettier": "^3.0.0",
    "husky": "^1.1.3",
    "lint-staged": "^8.0.4",
    "parcel-bundler": "^1.10.3",
    "prettier": "^1.15.1"
  }
}
